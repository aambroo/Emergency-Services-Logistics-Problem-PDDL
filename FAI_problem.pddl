(define (problem fai) (:domain classical_planning)
(:objects 
)

(:init
    (in ?x ?y ?room)
    
)

(:goal (and
    ;todo: put the goal condition here
))

;un-comment the following line if metric is needed
;(:metric minimize (???))
)
